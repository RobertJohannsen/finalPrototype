using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class lootValues : MonoBehaviour
{
    public int ammo, hp, maxhp, att, def, critmod, acc ,agi ,coin;
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
